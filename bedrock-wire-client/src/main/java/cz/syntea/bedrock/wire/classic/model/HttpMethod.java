package cz.syntea.bedrock.wire.classic.model;

/**
 * Supported HTTP methods.
 * Values are uppercase per RFC 7230 §3.1.1.
 */
public enum HttpMethod {
    GET, POST, PUT, DELETE, HEAD, PATCH, OPTIONS
}